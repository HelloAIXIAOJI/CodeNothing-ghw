using lib <io>;
using ns std;

fn main() : int {
    // 测试println函数
    println("这是从库函数打印的消息");
    
    // 测试print函数
    print("输入一些内容: ");
    
    // 测试read_line函数
    // input : string = read_line();
    input : string = "这并非来自read_line，这是测试"; // 测试
    println("你输入了: " + input);

    // 测试printf函数和转义字符
    printf("printf测试，换行符\n制表符\t反斜杠\\引号\"");
    
    // 测试带格式化参数的printf
    printf("\n\n格式化打印测试: %s, %d, %f\n", "字符串参数", "42", "3.14");
    
    // 也可以使用完整命名空间路径
    std::println("这是使用完整命名空间路径调用的函数");
    
    // 返回一个值
    result : int = 0;
    return result;
};