fn main() : int {
    a : int = 20;
    b : int = 6;
    
    a++;      // 自增，a变为21
    a--;      // 自减，a变为20
    
    a += 5;   // 加法赋值，a变为25
    a -= 3;   // 减法赋值，a变为22
    a *= 2;   // 乘法赋值，a变为44
    a /= 4;   // 除法赋值，a变为11
    a %= 5;   // 取模赋值，a变为1
    
    return a;
}; 