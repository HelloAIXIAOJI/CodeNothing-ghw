fn main() : int {
    // 整数运算
    i1 : int = 10;
    i2 : int = 3;
    i_add : int = i1 + i2;      // 13
    i_sub : int = i1 - i2;      // 7
    i_mul : int = i1 * i2;      // 30
    i_div : int = i1 / i2;      // 3
    i_mod : int = i1 % i2;      // 1
    
    // 浮点数运算
    f1 : float = 10.5;
    f2 : float = 3.5;
    f_add : float = f1 + f2;    // 14.0
    f_sub : float = f1 - f2;    // 7.0
    f_mul : float = f1 * f2;    // 36.75
    f_div : float = f1 / f2;    // 3.0
    
    // 混合运算
    mix1 : float = i1 + f2;     // 13.5
    mix2 : float = f1 * i2;     // 31.5
    
    // 长整型运算
    l1 : long = 1000000000000;
    l2 : long = 2000000000000;
    l_add : long = l1 + l2;     // 3000000000000
    
    // 字符串连接
    s1 : string = "Hello, ";
    s2 : string = "World!";
    s_add : string = s1 + s2;   // "Hello, World!"
    
    // 数组
    arr : int[] = [1, 2, 3];
    
    // 映射
    map : Map<string, int> = {"one": 1, "two": 2};
    
    return 0;
}; 