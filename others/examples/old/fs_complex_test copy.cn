// 导入文件系统库和标准IO库
using lib <fs>;
using lib <io>;
using ns std;

fn main() : int {
    println("当前工作目录: " + dir::current());
    
    // 创建一个测试目录
    test_dir : string = "fs_test_dir";
    dir::create(test_dir);
    
    return 0;
}; 