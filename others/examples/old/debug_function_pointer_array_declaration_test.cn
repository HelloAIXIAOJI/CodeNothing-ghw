using lib <io>;
using ns std;

// 调试函数指针数组声明测试

fn add(a : int, b : int) : int {
    return a + b;
};

fn main() : int {
    std::println("=== 调试函数指针数组声明测试 ===");
    
    // 测试函数指针数组类型声明
    operations : []*fn(int, int) : int;
    std::println("函数指针数组声明成功");
    
    std::println("=== 测试完成 ===");
    return 0;
};
