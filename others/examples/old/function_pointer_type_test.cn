using lib <io>;
using ns std;

// 函数指针类型声明测试

fn main() : int {
    std::println("=== CodeNothing 函数指针类型声明测试 ===");
    
    // 测试函数指针类型声明
    mathFunc : *fn(int, int) : int;
    stringFunc : *fn(string) : string;
    simpleFunc : *fn() : int;
    voidFunc : *fn(int) : void;
    
    std::println("✓ 函数指针类型声明成功");
    std::println("  mathFunc : *fn(int, int) : int");
    std::println("  stringFunc : *fn(string) : string");
    std::println("  simpleFunc : *fn() : int");
    std::println("  voidFunc : *fn(int) : void");

    std::println("");
    std::println("=== 函数指针类型声明测试完成 ===");
    return 0;
};
