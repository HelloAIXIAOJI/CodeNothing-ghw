// 04_import_statements.cn - 导入语句测试
using lib <io>;
using lib <time>;
using ns std;

fn main() : int {
    std::println("=== 导入语句测试 ===");
    
    // 1. 库导入测试
    std::println("1. 库导入测试");
    std::println("io库导入成功 - println 可用");
    
    // 2. 命名空间导入测试
    std::println("2. 命名空间导入测试");
    std::println("std命名空间导入成功");
    
    // 3. 测试导入的函数
    std::println("3. 导入函数测试");
    println("直接调用导入的println");
    
    std::println("=== 导入语句测试完成 ===");
    return 0;
};
