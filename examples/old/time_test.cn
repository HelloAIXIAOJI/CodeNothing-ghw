using lib <io>;
using ns std;

// 简单的时间测试程序
fn main() : int {
    std::println("时间测试程序");
    
    // 简单计算
    sum : int = 0;
    for (i : 0..1000) {
        sum = sum + i;
    };
    
    std::println("计算结果: " + sum);
    return 0;
};
