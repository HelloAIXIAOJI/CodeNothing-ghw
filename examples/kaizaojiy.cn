using lib <os>;
using lib <io>;
using lib <time>;
using ns std;

fn main() : int {
    while (true) {
        exec("powershell", "-Command", "Clear-Host");
        println("  🎃");
        println("██░░██");
        println("  ░░");
        sleep_seconds(0.2);
        exec("powershell", "-Command", "Clear-Host");
        println("  🎃");
        println("░░██░░");
        println("  ██");
        sleep_seconds(0.2);
    };
    return 0;
};
